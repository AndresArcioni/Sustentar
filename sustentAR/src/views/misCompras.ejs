<!DOCTYPE html>
<html lang="en">

    <%- include('./partials/Head') %>

    <body>
        <%- include('./partials/Header') %>
        <div class="container-fluid">
            <div class="container">
                <div class="misCompras">
                    <h4>Mis compras</h4>
                    <% for(let i = 0; i < productosComprados.length; i++){ %>
                        <div class="productoComprado row">
                            <img class="col-3" src="../images/productos/Diversion24.jpg" alt="">
                            <div class="productInfo col-9">
                                <h5>Nombre del producto</h5>
                                <p>$$ Precio $$</p>
                            </div>
                        </div>
                    <% } %>
                    <% if(productosComprados.length == 0) %>
                        <h6>No has realizado compras</h6>
                    <% } %>
                </div>
            </div>
        </div>
        


        <%- include('./partials/footer.ejs') %>
    <%- include('./partials/scripts.ejs') %> 
    </body>
</html>